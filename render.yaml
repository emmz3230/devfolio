services:
  - type: web
    name: devfolio
    runtime: static
    rootDir: devfolio
    buildCommand: npm install && npm run build
    staticPublishPath: dist
    routes:
      - type: rewrite
        source: /*
        destination: /index.html